// ─── Dashboard Stats ──────────────────────────────────────────────

export interface MonthStats {
  bookingCount: number;
  unassignedCount: number;
  destinationCount: number;
  revenue: number;
}
